# Server Configuration
PORT=8080
BASE_URL=http://localhost:8080
ENVIRONMENT=development

# Database (Neon Postgres)
# Get connection string from https://neon.tech
DATABASE_URL=postgres://user:password@host.neon.tech/rhone?sslmode=require

# GitHub OAuth (for user login)
# Create OAuth App at https://github.com/settings/applications
# Callback URL should be: ${BASE_URL}/auth/callback
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# GitHub App (for repository access)
# Create GitHub App at https://github.com/settings/apps
# Callback URL should be: ${BASE_URL}/github/callback
# Permissions: Repository Contents (Read), Repository Metadata (Read)
GITHUB_APP_ID=123456
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_APP_SLUG=rhone-cloud

# Session Secret (must be at least 64 characters for AES-256 encryption)
# Generate with: openssl rand -hex 32 (run twice and concatenate)
SESSION_SECRET=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# CSRF Secret (at least 32 characters)
# Generate with: openssl rand -hex 16
CSRF_SECRET=0123456789abcdef0123456789abcdef

# Environment Variable Encryption Key (exactly 32 characters for AES-256-GCM)
# Generate with: openssl rand -base64 24 | head -c 32
# Optional in development (uses default key), required in production
ENV_ENCRYPTION_KEY=01234567890123456789012345678901
